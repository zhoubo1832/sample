
pipeline {
  agent any
  stages {
    stage('init') {
      echo "init..."
    }
    
    stage('checkout') {
      echo "checkout..."
    }
  
  }

  post {
    always {
      echo "post..."
    }
  }
}
